package gui;

import model.User;

import javax.swing.*;

public class TeachersRegisterFrame extends JFrame {
    public TeachersRegisterFrame(User user) {
        this.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        this.setSize(800,600);
        this.setTitle("Menu");
        this.setLocationRelativeTo(null);
        this.setLayout(null);
    }
}
